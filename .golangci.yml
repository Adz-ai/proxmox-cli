version: "2"

linters:
  settings:
    errcheck:
      # Terminal output errors are not actionable for a CLI.
      exclude-functions:
        - fmt.Fprint
        - fmt.Fprintf
        - fmt.Fprintln
